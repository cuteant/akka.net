<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\..\nuget.props" />

  <PropertyGroup>
    <TargetFrameworks>$(StandardDevTfms)</TargetFrameworks>
    <RootNamespace>Akka.Serialization</RootNamespace>
    <AssemblyName>Akka.Serialization.SpanJson</AssemblyName>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <DefineConstants>$(DefineConstants);SERIALIZATION</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(TargetFramework)' == 'net471' or '$(TargetFramework)' == 'net451' ">
  </PropertyGroup>

  <PropertyGroup>
    <PackageId>Akka.Serialization.SpanJson</PackageId>
    <Title>Akka.Serialization.SpanJson</Title>
    <Description>SpanJson serializer for Akka.NET</Description>
    <PackageTags>akka;actors;actor model;Akka;concurrency;json;serializer;serialization</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SpanJson.Extensions" Version="$(SpanJsonExtensionsPackageVersion)" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Akka.Core\Akka.Core.csproj" />
  </ItemGroup>

  <Import Project="..\..\version.props" />
</Project>